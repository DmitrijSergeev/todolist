const initialState = {}

export const tasksReducer = (state = initialState, action: any) => {
    switch (action.type) {
        case '': {
            return
        }
        default:
            return state
    }
}

