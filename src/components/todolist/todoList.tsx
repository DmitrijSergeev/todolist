import * as SelectRadix from "@radix-ui/react-select";

export const TodoList = () => {

    return (
        <div>
            Hello World
            <SelectRadix.Root
                name={'Hello'}
            >

            </SelectRadix.Root>
        </div>
    );
};
